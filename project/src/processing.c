#include "directory_processing.h"
#include "files_processing.h"
#include "processing.h"

int init_block(files_t *files, set_t *set, bag_of_words_t *bag, char *path_to_dir) {
    if (path_to_dir == NULL) {
        return -1;
    }
    files = read_files()

    return 0;
}

